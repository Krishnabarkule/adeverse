databricks-sdk==0.19.1  # Replace <version> with the appropriate version
boto3==1.28.0            # Replace <version> with the appropriate version
milvus==2.2.6          # Replace <version> with the appropriate version
botocore==1.29.76
